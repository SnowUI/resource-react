import type { AssetEntry } from "./types";

/**
 * 素材元数据列表
 * 
 * 此文件由 scripts/process-all.ts 自动生成，请勿手动编辑
 */
export const assets = <const>[
  {
    type: "avatars",
    name: "avatar-byewind",
    pascal_name: "AvatarByewind",
    alias: { name: "AvatarByewind", pascal_name: "AvatarByewind" },
    files: [
      { format: "png", path: "avatars/avatar-byewind.png" }
    ],
  },
  {
    type: "avatars",
    name: "cursors-beachball",
    pascal_name: "CursorsBeachball",
    alias: { name: "CursorsBeachball", pascal_name: "CursorsBeachball" },
    files: [
      { format: "svg", path: "avatars/cursors-beachball.svg" }
    ],
  },
  {
    type: "backgrounds",
    name: "gradient-01",
    pascal_name: "Gradient01",
    alias: { name: "Gradient01", pascal_name: "Gradient01" },
    files: [
      { format: "jpg", path: "backgrounds/gradient-01.jpg" },
      { format: "png", path: "backgrounds/gradient-01.png" }
    ],
  },
  {
    type: "cursors",
    name: "cursors-beachball",
    pascal_name: "CursorsBeachball",
    alias: { name: "CursorsBeachball", pascal_name: "CursorsBeachball" },
    files: [
      { format: "svg", path: "cursors/cursors-beachball.svg" }
    ],
  },
  {
    type: "cursors",
    name: "cursors-text-cursor",
    pascal_name: "CursorsTextCursor",
    alias: { name: "CursorsText Cursor", pascal_name: "CursorsTextCursor" },
    files: [
      { format: "svg", path: "cursors/cursors-text-cursor.svg" }
    ],
  },
] satisfies readonly AssetEntry[];

/**
 * 根据类型和名称查找素材
 */
export function findAsset(type: string, name: string): AssetEntry | undefined {
  return assets.find(
    (asset) => asset.type === type && (asset.name === name || asset.alias?.name === name)
  );
}

/**
 * 根据类型获取所有素材
 */
export function getAssetsByType(type: string): AssetEntry[] {
  return assets.filter((asset) => asset.type === type);
}

/**
 * 获取所有素材类型
 */
export function getAllAssetTypes(): string[] {
  return Array.from(new Set(assets.map((asset) => asset.type)));
}
